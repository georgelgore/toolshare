<h1>Reservations</h1>
  <div style="border: 1px solid lightgray; padding: 20px; width: 300px">
    Customer: <%= link_to @reservation.user.name, user_path(@reservation.user) %><br><br>
    Item: <%= link_to @reservation.item.name, item_path(@reservation.item) %><br><br>
    <h4>Reservation Dates:</h4> <%= @reservation.start_date.strftime('%A, %B  %dth, %Y')%>  -  <%= @reservation.end_date.strftime('%A, %B  %dth, %Y')%><br><br>
    Total Cost: $<%= sprintf('%.2f', @reservation.total_cost) %><br><br>
  </div>
